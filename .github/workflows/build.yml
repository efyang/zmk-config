on: [push, pull_request, workflow_dispatch]

name: Build

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: zmkfirmware/zmk-build-arm:2.4
    name: Build
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Cache west modules
        uses: actions/cache@v2
        env:
          cache-name: cache-zephyr-modules
        with:
          path: |
            modules/
            tools/
            zephyr/
            bootloader/
            zmk/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('manifest-dir/west.yml') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-
      - name: West Init
        run: west init -l config
      - name: West Update
        run: west update
      - name: West Zephyr export
        run: west zephyr-export
      - name: West Build (Sweep Left)
        run: west build -s zmk/app -b nrfmicro_13 -- -DSHIELD=sweep_left -DZMK_CONFIG="${GITHUB_WORKSPACE}/config"
      - name: Sweep DTS File
        if: ${{ always() }}
        run: cat -n build/zephyr/nrfmicro_13.dts.pre.tmp
      - name: Sweep Left Kconfig file
        run: cat build/zephyr/.config | grep -v "^#" | grep -v "^$"
      - name: Rename zmk.uf2
        run: cp build/zephyr/zmk.uf2 sweep_left_nrfmicro_13.uf2
      - name: Archive (Sweep Left)
        uses: actions/upload-artifact@v2
        with:
          name: firmware
          path: sweep_left_nrfmicro_13.uf2
      - name: West Build (Sweep Right)
        run: west build --pristine -s zmk/app -b nrfmicro_13 -- -DSHIELD=sweep_right -DZMK_CONFIG="${GITHUB_WORKSPACE}/config"

      - name: Sweep Right Kconfig file
        run: cat build/zephyr/.config | grep -v "^#" | grep -v "^$"
      - name: Rename zmk.uf2
        run: cp build/zephyr/zmk.uf2 sweep_right_nrfmicro_13.uf2
      - name: Archive (Sweep Right)
        uses: actions/upload-artifact@v2
        with:
          name: firmware
          path: sweep_right_nrfmicro_13.uf2
